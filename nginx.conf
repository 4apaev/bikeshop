# Mountpoint: /var/lib/docker/volumes/backend_pub/_data
# Destination: /backend/pub

server {
  listen       80;
  server_name $SITE_NAME;

  gzip  on;
  root  /var/www/garage.dog/html;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }

  location /app {
    try_files $uri /index.html;
  }

  location /api {
    proxy_pass  http://backend:3000;
  }
}